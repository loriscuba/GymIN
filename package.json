{
  "name": "gymin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GymIN — gestionale palestra (Supabase + Mailpit)",
  "scripts": {
    "web": "npx --yes serve web -l 5173",
    "mail:dev": "node mailer/index.js watch",
    "mail:once": "node mailer/index.js once",
    "mail:test": "node mailer/index.js test",
    "mailpit:up": "docker compose up -d mailpit",
    "mailpit:down": "docker compose down",
    "ai": "node scripts/ai.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "nodemailer": "^6.9.14"
  },
  "devDependencies": {
    "playwright": "^1.46.0"
  }
}
